curr = *bucket;
    // prev = NULL;
    // while(curr)
    // {
    //     if(curr->etaStart.hrs < t.hrs)
    //     {
    //         prev = curr;
    //     }
    //     else if(curr->etaStart.hrs == t.hrs)
    //     {
    //         start = curr ;
    //         prev->next = NULL;
    //     }
    //     curr = curr->next;
    // }
    // curr->next = *bucket;
    // *bucket = start;